rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Milestone 0: deny by default. Domain-specific rules will be added later.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
